Principal Component Analysis (PCA)
==================================

The **nipalsPCA** class carries out principal component analysis. It analyses one data array and looks for systematic variance in 
the data using principal components (PC's). See below for a description of the methods in **nipalsPCA** as well as some examples 
of how to use it.

.. automodule:: hoggorm.pca
   :members: